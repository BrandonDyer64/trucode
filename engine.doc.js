
var keys = [
	/*
	These are the keys that are currently pressed.
	To see if a key is pressed you would use something
	like `keys[desiredKey]`
	Example:
		if (keys['W']) {
			// if the w key is pressed move up
			moveUp();
		}
	*/
];

var mouseX, mouseY;
	/*
	These hold the current x and y values of the mouse.
	*/

var mouseB = [
	/*
	These are the mouse buttons that are currently
	pressed. `mouseB[index]` will return whether or
	not a button is pressed. 0 - left, 1 - middle,
	2 - right.
	*/
];

var enableClear = true;







